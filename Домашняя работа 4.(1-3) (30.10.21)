#include <iostream>
using namespace std;
void print_array(int* array, int size) {
	for (int i = 0; i < size; i++) {
		cout << array[i] << " ";
	}
}
void task_1(int* array, int size, int diff) {
	for (int i = 0; i < size; i++) {
		array[i + 1] = array[i] + diff;
	}
	cout << "Арифметическая прогрессия: ";
	print_array(array, size);
}
void task_2(int* array, int N) {
	srand(time(0));
	for (int i = 0; i < N; i++) {
		array[i] = i+1;
	}
	print_array(array, N);
	cout << endl;
	cout << "Перестановка" << endl;
	for (int i = N; i > 1; i--) {
		int k = rand() % i;
		int s = array[i-1];
		array[i-1] = array[k];
		array[k] = s;
	}
	print_array(array, N);
}
void task_3(int* array, int N) {
	srand(time(0));
	int j = 0;
	for (int i = 0; i < N; i++) {
		array[i] = rand() % 1001 + 1000;
	}
	print_array(array, N);
	cout << endl;
	for (int i = 0; i < N; i++) {
		if (array[i] % 2 == 0) {
			j++;
		}
	}
	cout << "Количество четных чисел: " << j;
}
int main() {
	setlocale(LC_ALL, "Russian");
	int a;
	cout << "Введите № задачи" << " ";
	cin >> a;
	if (a == 1) {
		int size, diff;
		cout << "Введите размер массива ";
		cin >> size;
		cout << "Введите разность ";
		cin >> diff;
		int* array = new int[size];
		cout << "Введите первый элемент массива ";
		cin >> array[0];
		task_1(array, size, diff);
	}
	if (a == 2) {
		int N;
		cout << "Введите размер массива ";
		cin >> N;
		int* array = new int[N];
		array[0] = 1;
		task_2(array, N);
	}
	if (a == 3) {
		int N;
		cout << "Введите размер массива ";
		cin >> N;
		int* array = new int[N];
		task_3(array, N);
	}
	return 0;
}
